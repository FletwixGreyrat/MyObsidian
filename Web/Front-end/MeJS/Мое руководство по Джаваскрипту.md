ob# Кеширование браузером
`script.js?v=1`

# Строгий режим
"use strict"; в начале файла скрипта

# Переменные в JS
Латинские заглавные и строчные символы, андаскоры, доллар, цифры (не должны с них начинаться)


# Строки
"", '', \`\` (переносы, форматирование через ${})
str.length - длина строки

# Специальные значения
`undefined`, `null`, `true`, `false`, `NaN`, `Infinity`, `-Infinity`.


# Преобразование к числу
Number(), "+" перед значением (сокращенное)
parseInt() - выделит первое вхождение числа, даже если в строке есть лишние символы ("123sq234" -> 123)
parseFloat() - работает по аналогии


# Преобразование к строке
String()

# Преобразование к логическому типу

Boolean()

Особенности:
`console.log(Boolean(0)); // false` `console.log(Boolean(-0)); // false` `console.log(Boolean(+0)); // false` `console.log(Boolean(null)); // false` `console.log(Boolean(false)); // false` `console.log(Boolean(NaN)); // false` `console.log(Boolean(undefined)); // false` `console.log(Boolean('')); // false`

Все остальное - true

# Неизменяемость строк
Символы строк можно читать, но нельзя изменять. Следующий пример кода выдаст ошибку (в строгом режиме):

`let str = 'abcde';` 
`str[0] = '!';`


# Неточности вычисления
Дроби хранятся в компьютерах в таком виде, что часто могут быть представлены неточно. В этом случае вас может ждать некоторый сюрприз, когда банальная операция выдает странный результат. Пример:

`let a = 0.1 + 0.2;` 
`alert(a); // выведет 0.30000000000000004`

Для фикса нужно использовать n.toFixed()


# Функция promt
ввод данных prompt(), возвращает строку


# Вывод текста в документ
document.write()


# Изменяемость массивов по индексу
Массивы, в отличие от строк, можно изменять по индексу


# Добавление и удаление элементов массива
arr.push()
delete arr[index]

# Объекты в JS
Питоновский dict, ассоциативный массив, хэш-таблица

alert не выводит содержимое объекта, а console.log() выводит

Строковые ключи можно не брать в кавычки, если ключ не начинается с цифры и не имеет специальных символов

Элементы можно изменять по ключу

Object.keys(obj) - ключи объекта 

Можно искать ключи через in (bool)

delete obj[key]

Объекты и массивы проверяются через Array.isArray(obj)

Примитивные объекты являются ссылками, объекты же независимы


# Условия
if () {

}

else if () {

}

else{

}

switch (переменная) {
	case "значение1":
		код
	break;
	case "значение2":
		код
	break;
	default: // все остальные случаи
		код
	break;
}

Тернарный оператор 
`let age = 17;`
`let adult = age >= 18   ? true: false;`  

`console.log(adult);`


# Операторы сравнения
\>
\<
\== - нестрогое значение (акцент на значение)
!= - нестрогое неравенство (акцент на значение)
\=== - строгое сравнение (акцент на тип данных)
!== - строгое неравенство (акцент на тип данных)

&& - и
|| - или
! - не


# confirm
Функция `confirm` вызывает окошко с вопросом, на который нужно ответить пользователю, и двумя кнопками для ответа: с кнопкой `'ОК'` и с кнопкой `'Отмена'`. Если пользователь нажмет `'ОК'` - то функция вернет `true`, а если `'Отмена'` - то вернет `false`.

`let ok = confirm('Текст   вопроса');`  `console.log(ok);`


# for-of
Цикл `for-of` позволяет последовательно перебирать элементы массивов. Он имеет следующий синтаксис:

for (let n of arr){

} - для массивов


# for-in

for (let n in obj){

} - для объектов


# while 
while () {

}

# for

for (начальная команда; условие завершения; команда после прохода) {

}


# Степени 
Math.pow(число, степень);

# Корень
Math.sqrt() - квадратный корень


# Округление
Math.round() -  по правилам математики

Math.ceil() - округление дробного числа в большую сторону 

Math.floor() - округление дробного числа в меньшую сторону 

num.toFixed(n) - округление числа до n-го знака после запятой

num.toPrecision(n) - округляет до заданного знака

# Минимум и максимум
Math.max() - возвращает максимум из числа. Можно находить максимум в массиве, с помощью "распаковки" 
Math.max(...arr); При пустом значении max выведет Infinity. Можно также работать с массивами и значениями через Math.max.apply(null, arr);

Math.min() - возвращает минимум или -Infinity

# Random
`return Math.random()   * (max - min)   + min;`

`return Math.floor(Math.random()   * (max - min   + 1)) + min;`


# Модули
Math.abs()


# Регистры
str.toUpperCase() - все большими
str.toLowerCase() - все маленькими
Также используя метод slice можно преобразовать в верхний регистр отдельные буквы:

`let str = 'abcde';` `let res = str.slice(0,   1).toUpperCase()   + str.slice(1);`


# Подстроки
`let str = 'abcde';`
`let sub = str.substr(0, 3);`

`let sub = str.substring(0, 3);`

`let sub = str.slice(0, 3);`


# Поиск по строкам
str.includes(что ищем, откуда(индекс,опционально));
str.startsWith(что ищем, \[начало проверки]);`
str.endsWith(что ищем, \[длина строки]);`
str.indexOf(что ищем, \[откуда начинать поиск]); - возвращает индекс первого вхождения или -1
str.lastIndexOf(что ищем, \[откуда   начинать   поиск]); - Возвращает индекс последнего вхождения или -1


# Изменения в строке
str.replace(что заменяем, на что заменяем);
str.split(\[разделитель], \[максимальное   количество   элементов]);

# Изменение массивов
arr.join(\[разделитель]);
arr.shift(); - удаляет первый элемент массива
arr.pop(); - удаляет последний элемент массива
arr.push(элемент, элемент, элемент...); - добавляет неограниченное количество элементов в массив
arr.push(элемент, элемент, элемент...); - добавляет неограниченное количество элементов в начало массива

arr.slice(откуда отрезать, \[докуда   отрезать]) - вырезает и возвращает указанную часть массива. Сам массив при этом не изменяется


# Функции
function func {
 //
}

function func (num, num1=0) {
  return num + num1;
}


# Советы по созданию функций

1. Имена функций должны быть глаголами.
2. Имя каждой функции должно точно отражать то, что функция делает.
3. Функция должна делать только то, что явно подразумевается её названием, и не делать другого.
4. Каждая функция должна выполнять только одно действие.
5. Используйте внутри функций вспомогательные функции.
6. Код функций лучше не делать длиннее `10`-`15` строк.
7. Длинные функции лучше разбивать на ряд вспомогательных.
8. Используйте общепринятые префиксы в именах функций: `show`, `get`, `set`, `calc`, `create`, `change`, `check`.
9. Выносите дублирующийся код в функции.


# Дата и время
`let date = new Date();`

`console.log(date.getFullYear()); // год` `console.log(date.getMonth()); // месяц` (январь - нулевой) `console.log(date.getDate()); // день` `console.log(date.getHours()); // часы` `console.log(date.getMinutes()); // минуты` `console.log(date.getSeconds()); // секунды`
`console.log(date.getDay())` // день недели с нуля и вс

`let date = new Date(2015, 11, 4, 23, 59, 59);` `console.log(date.getTime());` - timestamp



# DOM события
dblclick
click
mouserover
mouseout
`image.addEventListener('load', function() {` `document.body.appendChild(image);` `});` `image.addEventListener('error', function() {` `// ошибка загрузки картинки` `});`

	focus - фокусировка текстовых полей

elem.textContent - читает текст
elem.innerHTML - читает разметку внутренности элемента (теги форматирования и прочее)
elem.id - обращение по атрибуту
elem.value - изменение значения инпута
elem.className - доступ к атрибуту class
htmlFor - доступ к атрибуту for

elem.getAttribute("value") - поиск по атрибуту

Метод `setAttribute` позволяет изменить значение заданного атрибута какого-либо тега.

`элемент.setAttribute(имя атрибута, новое значение);`
Метод `removeAttribute` удаляет заданный атрибут у какого-либо тега.

Метод `hasAttribute` проверяет наличие заданного атрибута у элемента. Если атрибут есть - выведет `true`, если нет - выведет `false`.

`<div id="elem" data-num="1000"></div>` 

В HTML разрешено добавлять свои, пользовательские атрибуты тегам. Такие атрибуты должны начинаться с `data-`, а затем должно идти любое название атрибута, которое вам удобно.

`let elem = document.querySelector('#elem');` `console.log(elem.dataset.num); // выведет 1000`

`<div id="elem" data-my-test="1000"></div>`

Для обращения к таким атрибутам следует использовать camelCase:

`let elem = document.querySelector('#elem');` `console.log(elem.dataset.myTest);`

elem.classList - псевдомассив классов элемента
elem.classList.add()
elem.classList.remove()
elem.classList.contains() - проверка на наличие
elem.classList.toggle() - добавляет, если нет, удаляет, если есть

elem.style.color = "red"
elem.style.fontSize = "15px" изменение атрибута style

Свойство [`float`](https://code.mu/ru/markup/manual/css/property/float/) является исключением, так как оно является специальным в JavaScript. Для него следует писать `cssFloat`

elem.firstElementChild(селектор родителя) - первый потомок
elem.lastElementChild - последний потомок
elem.children - псевдомассив всех потомков
elem.parentElement(css селектор) - родительский элемент
elem.closest("selector") - ближайший родительский элемент, подходящий под указанный CSS селектор
elem.previousElementsSibling - предыдущий элемент в этом же родителе или null
elem.nextElementSubling
document.getElementById() - элемент по его айди
document.getElementsByTagName - элементы страницы по имени тэга
document.getElementByClassName - элементы страницы по имени класса

можно использовать по эти get'ы по elem'ам уже найденным

существуют также свойства `firstChild`, `lastChild`, `nextSibling`, `previousSibling`. Эти свойства работают аналогичным образом, однако, учитывая все узлы. (комментарии)

childNodes - все узлы-потомки

Все узлы имеют свойство `nodeName`. Для комментария оно содержит значение `'comment'`, для текстового узла - значение `'text'`, а для узла-элемента - имя тега в верхнем регистре. Получается, что nodeName для элементов аналогичен свойству [`tagName`](https://code.mu/ru/javascript/manual/dom/tagName/), но работает также и для других типов узлов.

Все узлы имеют свойство `nodeType`. Его значение числовое: `1` для элементов, `3` для текстовых узлов, `8` для комментариев. Остальные значения в настоящее время или малоиспользуемы, или устарели.

nodeValue = textContent

В HTML существуют специальные атрибуты без значений, например, атрибут [`disabled`](https://code.mu/ru/markup/manual/html/attr/disabled/), используемый для блокировки элементов. Для того, чтобы установить такой атрибут, соответствующему свойству требуется присвоить значение `true`, а чтобы убрать - значение `false`.


событие `change`, возникающее в полях ввода при их изменениях. Что это значит? Пусть, к примеру, у вас есть инпут и в нем есть какой-то текст. Если вы измените этот текст, то в этом случае и возникнет это событие.

В селектах существует специальное свойство `selectedIndex`, хранящее в себе номер того пункта списка, который сейчас выбран. Нумерация при этом начинается с нуля. При этом данное свойство можно как прочитывать, так и записывать, меняя выбранный пункт списка.

При работе с селектами есть следующая особенность: переменная, в которой хранится ссылка на селект является массивом, элементами которого являются теги `option`. Это значит, что эту переменную можно перебрать циклом:

`console.log(option.text);`
`console.log(option.value);`
`console.log(option.selected);`


event.target
event.clientX
event.clientY
event.pageX
event.pageY

event.key
event.code

`ctrlKey`, `altKey`, `shiftKey`

event.preventDefault() - отмена действия тега по умолчанию

`stopPropagation` - прекращение всплытия

stopImmediatePropagation - не только предотвращает всплытие, но и останавливает обработку событий на текущем элементе. 

`createElement` - создание новых элементов
elem.remove() - удаление
append, prepend - вставка элементов в начало или в конец на JavaScript

`родитель.insertBefore(элемент, перед кем вставить);`


`list.appendChild(item);`


`button.addEventListener('click', function() {` `let item = document.createElement('li');` `item.textContent = 'item';` `item.addEventListener('click', function()   { // обработчик   клика`  `this.textContent = this.textContent + '!';` `});` `list.appendChild(item);` `});`



`function parent() {` `console.log(this.value); // выведет 'text'` `let child = () => {` `console.log(this.value); // выведет 'text'` `}` `child();` `}`

Это делается вот так: `func.call(elem)`. Этот код эквивалентен простому вызову функции `func` вот так: `func()`, только с условием, что this равен elem.

`function func(param1, param2) {` `console.log(this.value + param1 + param2);` `}`

`func.call(elem, param1, param2);`

`func.apply(elem, [param1, param2]);`


`let i = 0;` `let timerId = setInterval(function() {` `console.log(++i);` `if (i >= 10) {` `clearInterval(timerId);` `}` `}, 1000);`


`setTimeout` - позволяет сделать задержку перед запуском кода. Эта задержка, в отличии от функции setInterval, случится только один раз.


`let parent = document.querySelector('#parent');` `let p = document.createElement('p');` `p.textContent = '!';` `parent.appendChild(p);`

`let elems = document.querySelectorAll('p');` `for (let elem of elems) {` `elem.addEventListener('click', function() {` `elem.remove();` `});` `}`

`родитель.append(элемент или строка);` - позволяет вставить в конец какого-либо элемента другой элемент.

`родитель.prepend(элемент или строка);` - позволяет вставить в начало какого-либо элемента другой элемент

Метод `insertBefore` позволяет вставить элемент перед другим элементом - `родитель.insertBefore(элемент, перед кем вставить);`

`let p = document.createElement('p');` `p.textContent = '!';` `let target = document.querySelector('#target');` `target.insertAdjacentElement('beforeBegin', p);`

Метод `insertAdjacentElement` позволяет вставить элемент в любое место страницы

Метод `insertAdjacentHTML` позволяет вставить строку HTML кода в любое место страницы

`let parent = document.querySelector('#parent');` `let elem = parent.querySelector('.elem');` `let clone = elem.cloneNode(true);` `parent.appendChild(clone);`

Клонировать элемент можно с помощью метода `cloneNode`. В этот метод нужно передавать параметром true либо false. Если передан true, то элемент клонируется полностью, вместе со всем атрибутами и дочерними элементами, а если false - только сам элемент.


Метод `matches` позволяет проверить, удовлетворяет ли элемент указанному CSS селектору.
`элемент.matches('селектор');`


Метод `contains` позволяет проверить, содержит ли один элемент внутри себя другой
`родитель.contains(элемент);`


# Высший учебник

`elem.style.display = '';` - возврат к исходному значению

Можно задать сразу несколько стилей одной командой. Это делается с помощью свойства `cssText`:

`` elem.style.cssText = ` `` `width: 100px;` `height: 100px;` `display: block;` `` `; ``


`let computedStyle = getComputedStyle(elem);` - чтение css из файла. Размеры возвращает в абсолютной величине

Следующие свойства содержат клиентский размер элемента, включающий в себя размер содержимого и внутреннего отступа, и не включающего в себя полосу прокрутки:

`console.log(elem.clientWidth); // ширина` `console.log(elem.clientHeight); // высота`

Следующие свойства содержат полный размер элемента:

`console.log(elem.offsetWidth); // ширина` `console.log(elem.offsetHeight); // высота`

Следующие свойства содержат размеры элемента с учетом спрятанной под прокруткой части:

`console.log(elem.scrollWidth); // ширина` `console.log(elem.scrollHeight); // высота`

Следующие свойства содержат то, на сколько прокручен элемент:

`console.log(elem.scrollTop); // сверху` `console.log(elem.scrollLeft); // слева`

`elem.scrollTop = 200;` - прокручивание




Следующие свойства содержат размеры окна браузера, не включающие в себя полосу прокрутки:

`let w = document.documentElement.clientWidth; // ширина`

`let h = document.documentElement.clientHeight; // высота`

Следующие свойства включают в себя полосу прокрутки:

`let w = window.innerWidth; // ширина`

`let h = window.innerHeight; // высота`

Разница между двумя типами свойств дает размер полосы прокрутки:

`let w1 = document.documentElement.clientWidth;` `let w2 = window.innerWidth;` `console.log(w2 - w1);`


Вникать в них не стоит, просто используйте.

Высота с учетом прокрученной части:

`let scrollHeight = Math.max(` `document.body.scrollHeight, document.documentElement.scrollHeight,` `document.body.offsetHeight, document.documentElement.offsetHeight,` `document.body.clientHeight, document.documentElement.clientHeight` `);`

`console.log(scrollHeight);`

Ширина с учетом прокрученной части:

`let scrollWidth = Math.max(` `document.body.scrollWidth, document.documentElement.scrollWidth,` `document.body.offsetWidth, document.documentElement.offsetWidth,` `document.body.clientWidth, document.documentElement.clientWidth` `);` `console.log(scrollWidth);`



Следующие свойства содержат то, на сколько прокручено окно (только для чтения):

`let x = window.pageXOffset; // слева` `let y = window.pageYOffset; // сверху`

Следующие свойства содержат то, на сколько прокручено окно:

`let t = document.documentElement.scrollTop; // сверху`

`let l = document.documentElement.scrollLeft; // слева`

Эти свойства можно изменять, прокручивая окно в произвольную позицию:

`document.documentElement.scrollTop = 200;`


Метод `scrollTo` прокручивает окно в заданное положение. Первым параметром он принимает положение по горизонтали, а вторым - по вертикали. Давайте прокрутим окно к заданному положению:

`window.scrollTo(100, 200);`

Метод имеет альтернативный синтаксис, в котором параметром передается объект. В ключе `top` этого объекта указывается положение по вертикали, в ключе `left` - положение по горизонтали, а в ключе `behavior` - тип прокрутки. Тип может быть `'auto'` или `'smooth'`. Во втором случае прокрутка будет плавной:

`element.scrollTo({` `top: 100,` `left: 100,` `behavior: 'smooth'` `});`



Метод `scrollBy` прокручивает окно на заданную величину от текущего положения. Первым параметром он принимает сдвиг по горизонтали, а вторым - по вертикали. Давайте прокрутим окно на заданные величины:

`window.scrollBy(100, 200);`

Выполним прокрутку в обратную сторону:

`window.scrollBy(-100, -200);`

Выполним плавную прокрутку:

`window.scrollBy({` `top: 100,` `left: 100,` `behavior: 'smooth'` `});`

Метод `scrollIntoView` прокручивает окно к заданному элементу. Метод применяется к элементу, к которому следует прокрутить окно. Параметром метода регулируют, где должен оказаться элемент: сверху окна или снизу.

Давайте посмотрим на примерах. Прокрутим окно так, чтобы элемент был сверху окна:

`elem.scrollIntoView(true);`

А теперь прокрутим окно так, чтобы элемент был снизу окна:

`elem.scrollIntoView(false);`

Можно также параметром передать объект с настройками. Давайте плавно прокрутим окно к элементу:

`elem.scrollIntoView({` `behavior: 'smooth',`

`});`

Объект с настройками имеет также настройки выравнивания страницы по отношению к элементу. Настройка `block` задает вертикальное выравнивание, а настройка `inline` - горизонтальное. Они могут принимать следующие значения: `'start'`, `'center'`, `'end'`, `'nearest'`.

Давайте попробуем:

`elem.scrollIntoView({` `behavior: 'smooth',` `block: 'start',` `inline: 'end',` `});`


## Map

Коллекции `Map` представляют собой объекты, в которых в качестве ключей могут выступать как примитивы, так и объекты.

let map = new Map

map.set(key, value)

map.get(arr1)

map.size - размер 
map.has(arr1) - проверка на наличие
map.delete(arr1) - удаление
map.clear() - очистка 

Коллекции перебираются через for-of. При этом в `elem` будут попадать массивы пар ключ-значение. В первом элементе будет ключ, а во втором - значение

map.values(); - только значения коллекции
map.keys() - только ключи коллекции
map.entries() - пары ключ-значение в массиве



## Set
питоновское множество

`let set = new Set;`
`set.add(1);` 
`set.add(2);` 
`set.add(3);`


`let set = new Set([1, 2, 3, 3, 4, 4, 5]);`
`console.log(set);`

`set.size`
`set.has(1)`
`set.delete(1);`
`set.clear();`

for-of

`let arr = [...set];` - преобразование в массив
`let arr = Array.from(set);` - преобразование в массив


## Symbol

Тип `Symbol` предназначен для создания уникальных идентификаторов. Эти идентификаторы могут быть использованы для создания уникальных ключей объектов.

`let sym = Symbol();`
`console.log(sym);`


Созданные нами символы будут уникальными. Это значит, что при сравнении они не будут равны



С помощью метода `Symbol.for` можно создавать символы с именем. Эти символы будут сохранены в глобальный реестр символов и доступ к ним можно будет получить из любого места кода.

Имея переменную с символом, можно получить его имя в глобальном реестре. Это делается с помощью метода `Symbol.keyFor`

Существуют специальные встроенные символы, которые используются JavaScript в служебных целях. Такие символы называются well-known symbols (хорошо известные символы).

Примером такого символа служит `Symbol.iterator`, содержащий в себе функцию, перебирающую массивы

## Хранилища
`localStorage.setItem('key', 'text');`

`let str = localStorage.getItem('key');`
`console.log(str); // выведет 'text'`

`localStorage.removeItem('key');`
`localStorage.clear();`

`let num = localStorage.length;`


Каждая запись в локальном хранилище имеет свой номер. По номеру можно получить ключ этой записи:

`let key = localStorage.key(0);`

Можно получить массив ключей и значений всех записей из локального хранилища:

`let keys = Object.keys(localStorage);`

`let values = Object.values(localStorage);`


## Исключения

try{

} catch (error){

}

`throw new Error('текст исключения');`
`throw {name: 'MyError', message: 'текст исключения'};`

