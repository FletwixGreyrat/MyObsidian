
### **Домашнее задание по HTML**

---

#### **1. Медиаконтент (видео, аудио, фреймы)**  
1. **Теория**:  
   - Какие атрибуты тега `<video>` необходимы для отображения элементов управления плеером?  
   - Почему `autoplay` может не работать без атрибута `muted`?  
   - Чем отличается `<video>` от `<audio>`?  

2. **Практика**:  
   - Создай видео с автоматическим воспроизведением (без звука), шириной 600px и обложкой `preview.jpg`.  
   - Добавь аудиоплеер с зацикленным воспроизведением.  

3. **Сравнение**:  
   - Зачем нужен атрибут `poster`? Что отобразится, если его не указать?  

4. **Ошибки**:  
   - Найди ошибку:  
     ```html
     <audio src="music.mp3"></audio>
     ```  

5. **Встраивание контента**:  
   - Вставь карту Google Maps через `<iframe>` (ширина 100%, высота 400px).  

6. **Мобильная адаптация**:  
   - Объясни, зачем нужен атрибут `playsinline` для видео на мобильных устройствах.  

7. **Дополнительно**:  
   - Как сделать, чтобы видео начинало воспроизводиться только после клика?  

8. **Атрибуты**:  
   - Укажи корректные размеры для `<iframe>` с YouTube-видео (800x450).  

---

#### **2. Формы и поля ввода**  
1. **Теория**:  
   - Для чего нужны атрибуты `action` и `method` в теге `<form>`?  
   - Почему `<label>` важен для доступности формы?  

2. **Практика**:  
   - Создай форму с полем ввода имени (тип `text`), где `name="username"`.  
   - Добавь группу полей (`<fieldset>`) с заголовком "Контактные данные".  

3. **Связка элементов**:  
   - Свяжи `<label>` с `<input>` через атрибуты `for` и `id` (пример: "Email").  

4. **Ошибки**:  
   - Найди ошибку:  
     ```html
     <input type="text" id="age">
     <label>Возраст</label>
     ```  

5. **Серверная обработка**:  
   - Почему атрибут `name` обязателен для полей ввода?  

6. **Практика**:  
   - Создай форму с методом отправки `GET` и кнопкой "Отправить".  

7. **Группировка**:  
   - Чем `<fieldset>` полезен для структурирования форм?  

8. **Значение по умолчанию**:  
   - Укажи предзаполненное значение для поля "Город" (`value="Москва"`).  

---

#### **3. Типы полей ввода**  
1. **Теория**:  
   - Чем поле `type="email"` отличается от `type="text"`?  
   - Как `type="tel"` влияет на мобильную клавиатуру?  

2. **Практика**:  
   - Создай поле для ввода пароля с плейсхолдером "Введите пароль".  

3. **Валидация**:  
   - Сделай поле для ввода URL с автоматической проверкой формата.  

4. **Чекбоксы**:  
   - Добавь чекбокс "Согласен с условиями" (по умолчанию отмечен).  

5. **Радиокнопки**:  
   - Создай группу радиокнопок для выбора пола (Мужчина/Женщина).  

6. **Диапазон**:  
   - Добавь ползунок `type="range"` (min=0, max=100, значение=50).  

7. **Дата и время**:  
   - Вставь поле для выбора даты рождения (`type="date"`).  

8. **Загрузка файлов**:  
   - Создай поле для загрузки нескольких изображений (`type="file"`).  
