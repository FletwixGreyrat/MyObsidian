### **Домашнее задание по HTML**

---

#### **1.4: Форматирование текста**  
1. **Теория**:  
   - Чем отличаются теги `<b>` и `<strong>`? В каком случае использовать каждый из них?  
   - Объясни разницу между `<i>` и `<em>`. Приведи примеры их применения.  
   - Для чего предназначены теги `<s>`, `<u>`, `<sub>`, `<sup>`? Приведи по одному примеру для каждого.  
   - Зачем нужен тег `<mark>`? Как он отображается по умолчанию?  
   - Почему для переноса строки используют `<br>`, а не просто добавляют пробелы в HTML?  
   - В чем разница между `<code>` и `<pre>`? Когда использовать каждый из них?  
   - Когда стоит использовать `<hr>`, а когда лучше нарисовать линию через CSS?  
   - Какой тег уменьшает размер текста? Приведи пример его использования.  

2. **Практика**:  
   - Создай абзац, где часть текста будет **жирной** (с помощью `<b>`), а другая — *курсивом* (с помощью `<em>`).  
   - Напиши формулу воды (H₂O) и уравнение E=mc², используя подстрочные и надстрочные индексы.  
   - Создай стихотворение из 2 строк с принудительным переносом строки через `<br>`.  
   - Добавь горизонтальную линию между двумя абзацами текста.  
   - Выдели фрагмент кода: `console.log("Hello, World!");` с помощью `<code>`.  
   - Создай блок с текстом, где пробелы и переносы сохраняются (используй `<pre>`).  
   - Зачеркни старую цену товара и подчеркни новую.  
   - Выдели ярким фоном ключевое слово в предложении.  

---

#### **1.5: Ссылки и кнопки**  
1. **Теория**:  
   - Чем отличается тег `<a>` от `<button>`? Когда использовать каждый?  
   - Как сделать, чтобы ссылка открывалась в новой вкладке?  
   - Для чего нужен атрибут `href`? Какие значения он может принимать?  
   - Что делает атрибут `download` в теге `<a>`?  
   - Объясни, как создать якорную ссылку (например, переход к разделу "Контакты").  
   - Какие значения может принимать атрибут `type` у тега `<button>`?  
   - Почему кнопкам в формах нужно указывать `type="button"`?  
   - Как создать ссылку на email или телефон?  

2. **Практика**:  
   - Создай ссылку на сайт `https://example.com`, которая открывается в новой вкладке.  
   - Создай кнопку с текстом "Отправить" и типом `submit`.  
   - Сделай якорную ссылку, которая ведет к разделу с `id="footer"`.  
   - Добавь ссылку для скачивания файла `report.pdf`.  
   - Создай кнопку, которая сбрасывает данные формы (`type="reset"`).  
   - Сделай ссылку на email: `contact@example.com`.  
   - Создай кнопку с текстом "Показать пароль" и типом `button`.  
   - Добавь ссылку на номер телефона: `+7 (999) 123-45-67`.  

---

#### **1.6: Изображения**  
1. **Теория**:  
   - Какие атрибуты обязательны для тега `<img>`?  
   - Для чего нужен атрибут `alt`? Что произойдет, если его не указать?  
   - Объясни разницу между относительными и абсолютными путями.  
   - Как указать размер изображения через HTML? Почему это важно?  
   - Что делает атрибут `loading="lazy"`?  
   - Когда стоит оставлять атрибут `alt` пустым?  
   - Как добавить изображение из папки `images`, если текущий файл находится в корне проекта?  
   - Какие форматы изображений поддерживаются браузерами?  

2. **Практика**:  
   - Добавь изображение `cat.jpg` с альтернативным текстом "Кот".  
   - Укажи размер изображения: ширина 300px, высота 200px.  
   - Создай изображение с ленивой загрузкой.  
   - Используй абсолютный путь для изображения с URL `https://example.com/image.png`.  
   - Добавь изображение из папки `assets`, находящейся на уровень выше текущего файла.  
   - Создай изображение-ссылку: при клике на фото открывается `https://google.com`.  
   - Добавь изображение с пустым `alt` (декоративное).  
   - Исправь ошибку: `<img src="photo.jpg">` (нет закрывающего тега).  
