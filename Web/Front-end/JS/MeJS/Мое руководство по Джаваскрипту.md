# Кеширование браузером
`script.js?v=1`

# Строгий режим
"use strict"; в начале файла скрипта

# Переменные в JS
Латинские заглавные и строчные символы, андаскоры, доллар, цифры (не должны с них начинаться)


# Строки
"", '', \`\` (переносы, форматирование через ${})
str.length - длина строки

# Специальные значения
`undefined`, `null`, `true`, `false`, `NaN`, `Infinity`, `-Infinity`.


# Преобразование к числу
Number(), "+" перед значением (сокращенное)
parseInt() - выделит первое вхождение числа, даже если в строке есть лишние символы ("123sq234" -> 123)
parseFloat() - работает по аналогии


# Преобразование к строке
String()

# Преобразование к логическому типу

Boolean()

Особенности:
`console.log(Boolean(0)); // false` `console.log(Boolean(-0)); // false` `console.log(Boolean(+0)); // false` `console.log(Boolean(null)); // false` `console.log(Boolean(false)); // false` `console.log(Boolean(NaN)); // false` `console.log(Boolean(undefined)); // false` `console.log(Boolean('')); // false`

Все остальное - true

# Неизменяемость строк
Символы строк можно читать, но нельзя изменять. Следующий пример кода выдаст ошибку (в строгом режиме):

`let str = 'abcde';` 
`str[0] = '!';`


# Неточности вычисления
Дроби хранятся в компьютерах в таком виде, что часто могут быть представлены неточно. В этом случае вас может ждать некоторый сюрприз, когда банальная операция выдает странный результат. Пример:

`let a = 0.1 + 0.2;` 
`alert(a); // выведет 0.30000000000000004`

Для фикса нужно использовать n.toFixed()


# Функция promt
ввод данных prompt(), возвращает строку


# Вывод текста в документ
document.write()


# Изменяемость массивов по индексу
Массивы, в отличие от строк, можно изменять по индексу


# Добавление и удаление элементов массива
arr.push()
delete arr[index]

# Объекты в JS
Питоновский dict, ассоциативный массив, хэш-таблица

alert не выводит содержимое объекта, а console.log() выводит

Строковые ключи можно не брать в кавычки, если ключ не начинается с цифры и не имеет специальных символов

Элементы можно изменять по ключу

Object.keys(obj) - ключи объекта 

Можно искать ключи через in (bool)

delete obj[key]

Объекты и массивы проверяются через Array.isArray(obj)

Примитивные объекты являются ссылками, объекты же независимы


# Условия
if () {

}

else if () {

}

else{

}

switch (переменная) {
	case "значение1":
		код
	break;
	case "значение2":
		код
	break;
	default: // все остальные случаи
		код
	break;
}

Тернарный оператор 
`let age = 17;`
`let adult = age >= 18   ? true: false;`  

`console.log(adult);`


# Операторы сравнения
\>
\<
\== - нестрогое значение (акцент на значение)
!= - нестрогое неравенство (акцент на значение)
\=== - строгое сравнение (акцент на тип данных)
!== - строгое неравенство (акцент на тип данных)

&& - и
|| - или
! - не


# confirm
Функция `confirm` вызывает окошко с вопросом, на который нужно ответить пользователю, и двумя кнопками для ответа: с кнопкой `'ОК'` и с кнопкой `'Отмена'`. Если пользователь нажмет `'ОК'` - то функция вернет `true`, а если `'Отмена'` - то вернет `false`.

`let ok = confirm('Текст   вопроса');`  `console.log(ok);`


# for-of
Цикл `for-of` позволяет последовательно перебирать элементы массивов. Он имеет следующий синтаксис:

for (let n of arr){

} - для массивов


# for-in

for (let n in obj){

} - для объектов


# while 
while () {

}

# for

for (начальная команда; условие завершения; команда после прохода) {

}


# Степени 
Math.pow(число, степень);

# Корень
Math.sqrt() - квадратный корень


# Округление
Math.round() -  по правилам математики

Math.ceil() - округление дробного числа в большую сторону 

Math.floor() - округление дробного числа в меньшую сторону 

num.toFixed(n) - округление числа до n-го знака после запятой

num.toPrecision(n) - округляет до заданного знака

# Минимум и максимум
Math.max() - возвращает максимум из числа. Можно находить максимум в массиве, с помощью "распаковки" 
Math.max(...arr); При пустом значении max выведет Infinity. Можно также работать с массивами и значениями через Math.max.apply(null, arr);

Math.min() - возвращает минимум или -Infinity

# Random
`return Math.random()   * (max - min)   + min;`

`return Math.floor(Math.random()   * (max - min   + 1)) + min;`


# Модули
Math.abs()


# Регистры
str.toUpperCase() - все большими
str.toLowerCase() - все маленькими
Также используя метод slice можно преобразовать в верхний регистр отдельные буквы:

`let str = 'abcde';` `let res = str.slice(0,   1).toUpperCase()   + str.slice(1);`


# Подстроки
`let str = 'abcde';`
`let sub = str.substr(0, 3);`

`let sub = str.substring(0, 3);`

`let sub = str.slice(0, 3);`


# Поиск по строкам
str.includes(что ищем, откуда(индекс,опционально));
str.startsWith(что ищем, \[начало проверки]);`
str.endsWith(что ищем, \[длина строки]);`
str.indexOf(что ищем, \[откуда начинать поиск]); - возвращает индекс первого вхождения или -1
str.lastIndexOf(что ищем, \[откуда   начинать   поиск]); - Возвращает индекс последнего вхождения или -1


# Изменения в строке
str.replace(что заменяем, на что заменяем);
str.split(\[разделитель], \[максимальное   количество   элементов]);

# Изменение массивов
arr.join(\[разделитель]);
arr.shift(); - удаляет первый элемент массива
arr.pop(); - удаляет последний элемент массива
arr.push(элемент, элемент, элемент...); - добавляет неограниченное количество элементов в массив
arr.push(элемент, элемент, элемент...); - добавляет неограниченное количество элементов в начало массива

arr.slice(откуда отрезать, \[докуда   отрезать]) - вырезает и возвращает указанную часть массива. Сам массив при этом не изменяется


# Функции
function func {
 //
}

function func (num, num1=0) {
  return num + num1;
}


# Советы по созданию функций

1. Имена функций должны быть глаголами.
2. Имя каждой функции должно точно отражать то, что функция делает.
3. Функция должна делать только то, что явно подразумевается её названием, и не делать другого.
4. Каждая функция должна выполнять только одно действие.
5. Используйте внутри функций вспомогательные функции.
6. Код функций лучше не делать длиннее `10`-`15` строк.
7. Длинные функции лучше разбивать на ряд вспомогательных.
8. Используйте общепринятые префиксы в именах функций: `show`, `get`, `set`, `calc`, `create`, `change`, `check`.
9. Выносите дублирующийся код в функции.


# Дата и время
`let date = new Date();`

`console.log(date.getFullYear()); // год` `console.log(date.getMonth()); // месяц` (январь - нулевой) `console.log(date.getDate()); // день` `console.log(date.getHours()); // часы` `console.log(date.getMinutes()); // минуты` `console.log(date.getSeconds()); // секунды`
`console.log(date.getDay())` // день недели с нуля и вс

`let date = new Date(2015, 11, 4, 23, 59, 59);` `console.log(date.getTime());` - timestamp


